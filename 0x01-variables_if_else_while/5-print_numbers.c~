#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Prints numbers on base 10 from 0
 *
 * Return: Always success (0)
 */

int main(void)

{
int c;
for (c = 0; c <= 9; c++)
printf("d%", c);
printf("\n");
return (0);
}
