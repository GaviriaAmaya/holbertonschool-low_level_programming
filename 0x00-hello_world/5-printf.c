#include <stdio.h>
/**
 * main - Practice of printf. Also, -Wall on gcc
 * Return: 0 (Always success)
 */
int main(void)
{
	char *Dora = "with proper grammar, but the outcome is a piece of art,";

		printf("%s\n", Dora);

	return (0);
}
