#include <stdio.h>
/**
* Array: [] Coleccion, typado, tamano
*String: char variable [] = "Contenido"  /* Ultima pos. /0 */
*
*
*/
int main(void)
{
	char array[] = "Hola Mundo3";
	{
		int iterador = 0;

		do
		{
			iterador++;
			printf("%c\n", array[iterador];
		}
				while (array[iterador] != '\0');
	}
	return (0);
}
