#include "holberton.h"
/**
 *_strlen - Command that returns the length of a string
 *@s: String used
 *Return: Lenght of the string
 */
int _strlen(char *s)
{
	int n;
	if (s != NULL)
        {
	for (n = 0, *(n + s) != 0, n++;)
	{}
	}
	return (n);
}
